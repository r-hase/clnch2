素早いキーボードランチャ [ CraftLaunch ] 更新履歴

◎ 2.08

    VC7でのコンパイルはやめて、VC6でやる

◎ 2.07

  [修正]

	SJISの多バイト文字判定がまだおかしかったのを修正

◎ 2.06

  [修正]

	SJISの多バイト文字判定がおかしかったのを修正

◎ 2.04

  [機能追加]

    相対パスを絶対パスに変換して履歴に残す為のオプション、相対パスは絶対パ
    スに変換、を追加

    現在のマスクに置換される特殊変数 %AcceptMask、%IgnoreMask、を追加

    任意のコマンドを予約しておき、次回の Enter時のコマンド入力部の文字列全
    体をそのコマンドの引数として実行するモードに移行する内部命令
    &HiddenCommand を追加 (ファイラへの組み込み強化)

    &HiddenCommand で予約したコマンドを無効にして、通常モードに戻す内部命令
    &ClearHiddenCommand を追加

    置換文字列 %FullPath を追加

    3 回連続で同じキーでタイプミスしたら、タイプミスキャンセルを一時的に
    無効にするようにした ( &AllowTypeMiss 相当 )

    情報表示を切り替える内部命令、&ChangeInfo を追加

    擬似キーストロークを生成する内部命令 &Key、&KeyDown、&KeyUp を追加

    コマンドの実行を指定した時間だけ止める内部命令 &Sleep 追加

    エラーをログに残すようにした ( &PopLog で確認可能 )

    コマンド入力部にはエラーを出さないようにし、入力内容を保つようにした

    複数の引数は分解して履歴に残すオプションを追加

    リスト自動表示を一時的に無効にする内部命令 &AutoListOff を追加

    &TypeMissCancelOff を解除する &TypeMissCancelOn を追加
    &AutoCompleteOff を解除する &AutoCompleteOn を追加
    &AutoFixOff を解除する &AutoFixOn を追加
    &AutoListOff を解除する &AutoListOn を追加

    日本語の曜日表現に置換する置換文字列 %k を追加


  [調整]

    パスの区切りに [ / ] を使う、設定の時、履歴には \ を / に置換して残すよ
    うにした

    ファイラによるコマンドは残さない、オプションがいつのまにか常に On の動作
    になっていたのが判ったが、誰も困っていなかったので廃止

    単語区切り文字の入力による、適当な位置まで確定、の機能において、\ と /
    を同一視するようにした

    %FullPath[*] は、[ ] 内のファイルパスをフルパスに変換したものに置換され
    る &Pwd を廃止 (必要であれば &TextOut;%pwd が使用できる)

    &FixAll を廃止 (必要であれば &SetCaret;-1 が使用できる)

    情報表示機能の項目数を３つから１つに絞った
        ( ２つ目以降は &TextOut で代用していただく方針 )

    パスの区切りに [ / ] を使う、設定の時、ファイラから渡されるファイルパス
    の [ \ ] を自動的に [ / ] に変換するようにした

    スペースを含み、まだ存在しないファイル名を、新規作成等の目的でアプリに
    渡す際に、自動 Quote 処理が効かなかったので、ディレクトリが存在すれば、
    スペースを含むファイル名であると認識するようにした

    指定したの登録コマンドの Link 先に置換される特殊変数 %Link[] 追加

    コマンド設定ツールに、ソート機能をつけた

    clnchset.exe を clnchcmd.exe と改名した
        ( clnchset.exe は 削除してください )

    clnch.dat を clnch.opt と改名した
        ( clnch.opt が生成されたら clnchset.exe は 削除してください )

    &Setup は &SetCommand に改名

    &AllowTypeMiss を &TypeMissCancelOff に改名

    プロポーショナルフォントで、リスト表示の横幅が狭くなることがあったので
    調整

    %% を % に置換するのは、最後の一度だけにした

    %LinkDir など [] で引数をとるものは、まず [] の中を置換するようにして、
    ネスト出来るようにした


  [修正]

    アクティブであるにもかかわらず情報表示されてしまうことがある不具合を修
    正

    引数区切り文字で始まる履歴が、&RemoveHistory によって、そのままでは削除
    できなかった不具合を修正

    Delete のあと、リストのリフレッシュをしていなかった不具合を修正

    左右に大きくなる、を On にして リストを表示させた状態で &RemoveHistory
    すると、リストが左右に動いて見辛かったのを修正

    &SetFileMask が、手でマスクを入力した時もコントロール用命令 (コマンド入
    力部をクリアしない、履歴に追加しない、命令のタイプ) として実行されてい
    た不具合を修正

    クリップボードのテキストが %c 等を含んでいると、%cb が、クリップボード
    のテキストがさらに時計などの情報に置換される不具合を修正

    情報表示の内容が大きいと、時計が正常に表示されない不具合を修正

    ホットキーを押してもアクティブ化に時間がかかる時があった不具合を修正

    全角文字の削除が変だったのを修正

    &WinActivate を使ったときにリストが表示されていると、他のウインドウがア
    クティブ化しても、情報表示がスタートしない不具合を修正

    置換がコマンド本体部分に適用されていない個所があったのを修正

    ショートカットや空欄コマンドの中の %PWD が &PWD 勝手に変換される不具合
    を修正


◎ 2.01

    文字が収まるように大きくなる、の機能に関して、本体ウインドウの無駄な
    サイズ変更をなくした

    左右に大きくなる、が On の時、リストの選択項目を変更するとリストが
    左右に振動して見難かったのを修正


◎ 2.00

  [機能追加]

    コマンド入力部のフォントを設定できるようにした

    補完候補のリスト表示機能を追加

    スペースを含むパスに関して、方針的な変更
        1) スペースを含むパス内でもファイル名補完を出来るようにした
        2) 複数のファイルをひとつのコマンドに記述するには、引数区切り文字
           で区切っていく
        3) スペースを含むファイル名は実行前に自動的に " " で括られる

    任意の文字列を含む履歴をリスト表示する内部命令 &FilterHistory を追加

    登録コマンドの実行時に、末尾に [*] を付加することでオプションを指定出
    来るようにした

    登録コマンド実行時オプションとして、"Ctrl" "C" "Shift" "S" を用意した

    置換文字列 %LinkDir を追加
    %LinkDir[*] は、[ ] 内に指定したの登録コマンドのLink先のディレクトリ
    に置換される

    一時的にタイプミスを許可するモードにする特殊コマンド &AllowTypeMiss
    を追加

    一時的に自動補完をOFFにする特殊コマンド &AutoCompleteOff を追加

    一時的に自動確定をOFFにする特殊コマンド &AutoFixOff を追加

    キャレットの位置、または選択範囲を変更する内部命令 &SetCaret を追加
    ( &FixAllは削除予定なので、代わりに"&SetCaret;-1"を使ってください )


  [調整]

    /close つきで実行しているときにも、&Aboutが使えるようにした

    ↑↓キーによる履歴呼び出しで、確定部分が一致する履歴だけをリストアッ
    プするようにした

    &OpenLinkDir を廃止

    コマンド一覧メニューのコマンドにも、実行時に引数を渡すようにした

    設定ツールにシステムアイコンと最小化ボタンを追加した

    設定ツールがタスクバーに表示されるようにした

    ショートカットコマンドに Ctrl/Shift + Enter 等を登録すると登録コマンドの
    Ctrl/Shift + Enter が使えない問題があったが、登録コマンドに Ctrl / Shift
    との組み合わせが定義されていたら、登録コマンドを優先にするようにした

    &WinActivate 等のウインドウの識別では、アルファベットの大文字小文字を無
    視するようにした

    空欄コマンドやショートカットコマンドでは、[！] 無しでもファイル名実行が
    できるようにした

    重複する履歴を削除、の機能で、アルファベットの大小を無視して削除するよう
    にした

    &SetFileMask をショートカットに引数も含めて登録しておくことで、ファイル
    補完入力中でも使えるようにした

    タスクトレイアイコンにツールチップをつけた
    (なくても問題なかったが、CraftTrayで[No name]となって格好悪かった)

  [修正]

    ファイル名補完の無視マスクを空欄にすると、ファイルを補完しなくなって
    いた不具合を修正

    /close つきで実行しているときに、アプリの起動によるCraftLaunchの非ア
    クティブ化を検知してコマンドの実行を中止したり、不正な処理で落ちるこ
    とがあったのを修正

    BSによる全角文字の削除が１バイトだけだったのを修正

    Ctrl+BS/Delete による単語区切りまで削除する機能で、単語区切りまで１文
    字しかない場合に、次の単語区切りまで削除していた不具合を修正

    設定ツールで F1 キーを押すと、ヘルプファイルを探しにいっていたのを修正

    バージョン情報ダイアログを開いたときや&PopHistoryMenuから履歴を取得した
    後など、/wrで移動させた窓が定位置に戻る不具合を修正

    Edit Command ダイアログ、Shortcut List ダイアログを開いた状態で、Alt+Tab
    で CraftLaunch の設定ツールに戻ってくると、これらのダイアログが開いたま
    ま、親ウインドウがアクティブになってしまう不具合を修正

    CraftLaunchを画面の下の方に置いた状態でメニューが画面からはみ出してしま
    う不具合を修正

    &SubCommand 等がネストしたときに親の&SubCommandが&SubCommandForce扱いに
    なってしまう不具合を修正

    Windows98/2000で、CraftLaunchのウインドウがアクティブにならないことがあっ
    た不具合を修正

    起動時にウインドウを非表示にしない設定だと、環境によってはページ違反を起
    こして起動できなかった問題を修正

    ２バイト文字の中にある単語区切り文字相当のバイトを拾って単語区切りとして
    しまっていた不具合を修正

    コマンド設定ツールが、/ 区切りのパスに対応していなかったのを修正


◎ 1.98 :

    単語の区切りをきちんと処理して、Ctrl + ←/→ や Ctrl + BS/DEL で、
    適当な区切りまで処理するようにした
    ( ただし、Ctrl + ←/→ で単語区切り移動はNT/2000 のみ )

    ファイルの関連付け実行のとき、『開く』動作ではなく、ダブルクリック
    相当の動作をするようにした

    コマンド実行後サイズがデフォルトに戻らず、情報表示が始まらないこと
    があったのを修正

    単語の区切りとなる文字を入力したときの処理を、次のようにした

        1,カーソル位置に挿入して補完候補があるようであれば挿入
        2,選択範囲にその区切り文字があれば、最も近い区切り文字まで確定
        3,選択をすべて確定して、そのあとに挿入

    コマンドラインオプション /close をつけていても、コマンド実行後や、
    Escキー後に、CraftLaunch が終了しないことがあったのを修正

    %% が % に置換されるはずが、一部そうなっていなかったのを修正
    ( %%pwd と書いて、&SubCommandの中で %pwd を使う、といった用途 )

    %argの '/' を '\' に置換したバージョン %arg2 を追加

    1.94以降、2つ以上のファイル名をスペース区切りで入力するときの
    挙動が変になっていたのを修正

    ディレクトリを補完候補の先頭に集めるようにした

    ネットワークコンピュータ名と共有名は、時間がかかるので、列挙しないよう
    にした

    &SubCommandOrの中に&SubCommand系の特殊コマンドが使えない不具合を修正

◎ 1.96 :

    オプション、先頭に！でファイル名指定実行、が OFFのときに
    &OpenFiles が効かない不具合を修正

    &OpenFiles の引数を、カンマ区切りではなく、スペース区切りに変更
    ( ファイル名補完にあわせた )

    %arg が &SubCommandで階層化したコマンド呼び出しに対応していなかった
    のを修正

    情報切替えの一回目の入力は、切り替えないで表示するだけにした

    ショートカットなどのキー割り当てに F10 が使えなかった不具合を修正

    コマンド設定ツールのボタン群にはタブストップしないようにした

    ファイラからの呼び出しで /wr が無いと CraftLaunchが隠れてしまって
    いた不具合を修正

    カレントディレクトリを変更しても、内部で持っているファイル名補完
    リストが更新されないことがあったのを修正

    [ C: ]等でカレントディレクトリのファイル名が補完されていたのを、
    補完しないようにした

    ウインドウにキーストロークを送信する内部命令 &SendText を追加

    ファイル名補完で、パスの区切りに [ / ] を使う、オプションを追加

    ファイラから位置が変更されているとき、コマンド実行直後に、位置と
    サイズをデフォルトに戻すようにした

    先頭にスペースがある状態で補完が起動すると、補完後の選択位置が
    おかしくなっていた不具合を修正

    ショートカット一覧のウインドウサイズを変更可能にした

    ショートカット一覧から編集するショートカットを選択できるようにした

    設定ツールに [Apply] ボタンをつけた

    特殊変数と読んでいたものを置換文字列と呼ぶようにした

    登録コマンドの Link 欄にも置換文字列を使えるようにした
    ( Link に http://www.google.com/search?q="%arg" と書きたい人用 )

    環境変数を取得できるようにした
        ex) %env[PATH]

    内部変数の取得方法を変更した
        ex) %var[ca_CurTrc,--]

    &SubCommand の中では、[！] 無しでもファイル名実行ができるようにした

    ネストした置換文字列の処理がうまく処理できていなかったのを修正

    その他 修正/調整 など


◎ 1.94 :

    ファイルマスク機能の追加
    ( デフォルトマスクの設定と、%SetFileMaskによる変更が可能 )

    成功するまでコマンドを連続実行する内部命令、%SubCommandOr を追加

    途中で失敗しても、強制的にコマンドを連続実行する内部命令、
    %SubCommandForce を追加

    ウインドウの出現を待つ内部命令 %WaitWindow 追加

    内部命令の名前を % ではなく、& ではじめるようにした
    ( 設定ファイルは自動的に & に変換されますが、ファイラの設定などは
      各自で設定して頂く必要があります )

    &SubCommand の引数内で、ダブルクォートが使えるようにした
    ( 引数付きのコマンドの連続実行が可能 )

    引数区切り文字を入力しても、補完動作が起きないようにした
    "！"を入力しても、補完動作が起きないようにした
    ( すでに入力済みの文字が、補完によって消えてしまうのを防ぐ為 )

    Escキー相当の動作をする内部命令、&Escape を追加

    先頭に引数区切り文字を付加して、カーソルを先頭に移動させる内部命令、
    &UseAsArg を追加

    ファイルを開く内部命令 &OpenFiles 追加
    ( ショートカットに登録すると、先頭に！をつけなくてもファイルを開ける )

    %Pwd や %pWd など大文字小文字が混ざっていると駄目だったのを修正

    登録コマンドの Arg 欄が空欄じゃなかったら、入力の引数部分を追加しない
    ようにした ( かわりに下記の %arg を追加 )

    入力の引数部分に置換される特殊変数 %arg を追加
    ( フラグに追加的に引数を渡すとか、
      &SubCommand で連続実行するコマンドに、引数部分を渡すとか )

    空欄に置換される特殊変数 %null を追加
    ( 引数を受け取らないコマンドの Arg 欄に使う )

    ファイル名リストアップを高速化

◎ 1.92 :

    オプション設定ツールのキーコードを取得するコントロールで、すべての
    キー入力がキー設定に食われていたのを、モディファイア併用で回避でき
    るようにした

    文字が収まるように大きくなる、がON、かつ、ウインドウサイズが
    デフォルトの 87 のときに、文字入力でウインドウが小さくなる不具合
    を修正

◎ 1.91 :

    ショートカットの実体を登録したとき、ショートカットの設定内容を
    反映させるようにした

    隠しコマンド %SetValue , %KillValue で、常駐型プラグインに対応

    指定した文字数以下は履歴に残さない、を追加

    空欄コマンドとショートカットコマンドで、手で入力した部分は、履
    歴に残すようにした

    %RemoveHistory は、一部一致で消せるようにした

    CraftLaunchでEscを押して、他のウインドウがアクティブになるとき
    に最小化を解除してしまう不具合修正

    履歴から優先的に補完する、オプションを追加

    コマンド設定ツールのウインドウサイズを可変にした

    コマンド設定ツールの中で、コマンドをインクリメンタルサーチでき
    るようにした

    カーソル位置を末尾に移動させる特殊コマンド %FixAll を追加

    非常駐モードで設定ツールを起動したときは、設定ツール側から
    CraftLaunchを常駐させないようにした

    ファイル名補完で、ダブルクォートがが開かれているときは、スペース
    入りのパス以下でも補完できるようにした

    その他不具合修正


◎ 1.89 :
    %WinActivateで、最小化しているウインドウを元のサイズに戻すよう
    にした

    %WinActivate のあと、情報表示がスタートしないことがあったのを修正

    ショートカットコマンドのなかに引数区切り文字がある場合は、入力欄
    の文字列を引数として追加しないようにした

◎ 1.88 :
    オプション設定ツールで、正しい範囲外の値に対する警告が出るとき
    でも、ファイルに保存できていた不具合を修正

    ファイラの枠より大きくならない、オプション追加

    フォーカスを失ったらファイラ連携モード終了、オプション追加

    調整など

◎ 1.87 :
    設定ツールを、動作オプション設定用の clnchopt.exe と、コマンド
    設定用の clnchset.exe に分けた。起動するコマンドも、%SETUP と
    %OPTION に分けた。設定ファイルも clnch.dat と clnch.cmd に分けた

    枠の太さを変更できるようにした

    非アクティブのときも大きくなる、オプションを追加

    キー割り当ての設定で、出来るだけわかりやすいキーの名前を表示
    するようにした。

    ディレクトリ名無しは補完しない、オプションを追加
    │
    │ファイル名補完が意図しないところで効いて困る方はONにしてみて
    │ください。
    │
    │※ ただし、ファイラのプラグインモードでは、このオプションが
    │   無効となります。

    ショートカットコマンド機能を追加。
    │
    │Ctrl/Shift/Alt + 任意のキー をコマンド起動のショートカット
    │として設定できます。
    │
    │入力欄の文字はすべて引数としてコマンドに渡されます。

    コマンドのLink先の在処を開く特殊コマンド [ %OpenLinkDir ] 追加

    その他、調整 / 不具合修正など


◎ 1.85 :
    ファイラからのコマンドが一つだけだったときに何も起きない不具合
    を修正

◎ 1.84 :
    自動サイズで、変な場所に行ってしまうことがある不具合を修正

◎ 1.83 :
    ファイル名の補完機能を追加

    %SubCommand; %Exit ,%About で、強制終了していた不具合を修正


◎ 1.82 :
    常駐せずに終了するコマンドラインオプション /close を追加
    ( /close 付きで起動すると CraftLaunch の多重起動が可能 )

    タスクバーが常に手前に表示する設定だと、タスクバーを避けていた
    動作を、避けないようにした


◎ 1.81 :
    全角文字を Del / BS で削除するとハングする不具合を修正

    文字が収まるように大きくなる、が ON で、文字数が少なくなっ
    たときに、自動的に小さくなるようにした

    %TextOut や 情報表示切替えでも、自動的に大きくなるようにした

    文字が収まるように大きくなる、が ON でも、画面よりも大きく
    ならないようにした



◎ 1.80 :
   【 文字が収まるように大きくなる 】オプションを追加

    特殊コマンド【 %TextOut 】を追加

    情報表示に、%pwd %cb を使えるようにした
    コマンドの引数や作業Folderに、時計置換文字を使えるようにした

    Delキー、BSキー が相変わらずちょっと変だったのを修正


◎ 1.79 :
   【 状態表示部を隠す 】オプションを追加

    時計を表示しているとき、文字がちらつくことがあったのを修正

◎ 1.78 :
    Delキー、BSキーによる２バイト文字の削除が１バイトだけだった
    不具合の修正

    コマンド入力欄の、文字 / 背景の色、を設定できるようにした


◎ 1.77 :
    %WinActivateAll、上手くいかないので削除

    カーソル移動のコマンドラインオプション /c6 ～ /c9 を廃止、
    新しく /cA を新設した

    コマンドメニューのセパレータを意味する "-" が、補完や実行の
    対象になっていた不具合を修正

    細かい修正


◎ 1.76.1 :
    【 共通部分確定を自動的に 】を追加

    設定ツールで、矛盾する設定に警告ダイアログを出すようにした

    %SubCommand で 起動したコマンドは履歴に残さないようにした

    【単純なコマンドは履歴に残さない】オプションを追加

    【ファイラによるコマンドは残さない】オプションを追加

    命令 %PWD が、タイプミスを受け付けない、が ON のとき、正しく
    動作しない不具合を修正


◎ 1.74 :
   【 %WinActivateAll 】を追加

    %Setup の引数としてコマンド名を渡して、既存のコマンドの編集が
    出来るようにした

    細かい修正


◎ 1.72 :
    処理が重くなっていたので、こっそり修正

◎ 1.71 :
   【タイプミスを受け付けない】オプションが、自動補完OFFのときでも
    効くようにした

    空欄コマンドで登録コマンドを実行するとき、登録コマンドが
    Ctrl,Shift で切り替わる設定でも、Ctrl,Shift を無視するよう
    にした ( 空欄コマンド自体が Ctrl,Shift が切り替わるから )


◎ 1.70 :
   【前候補選択キー】の機能を追加

   【共通部分確定キー】の機能を追加 ( デフォルトで Ctrl+→ )

   【 タイプミスを受け付けない 】オプションを追加

    登録コマンド実行するときに、 Ctrl、Shift を押しておくことで、
    4 通りに使い分けることができるようにした

    ウインドウのタイトルとクラスで指定して、該当するウインドウを
    アクティブ化する特殊コマンド【%WinActivate】追加

    CraftLaunchのバージョン情報を表示する特殊コマンド【%About】追加

    引数で渡した履歴を削除する特殊コマンド【%RemoveHistory】追加
    履歴をすべて削除する特殊コマンド【%RemoveHistoryAll】追加

    %WinTitleMsgと%WinClassMsg を %WinMessageに統合
    %WinTitleMsgAllと%WinClassMsgAll を %WinMessageAllに統合
    ( 設定を変更する必要あり )

    コマンド入力部に対して、Ctrl + Del、Ctrl + BS、Ctrl + A、
    の操作を可能にした

    %WinTitleMsg(All)でウインドウ名を正しく発見できていなかった
    不具合を修正 ( メッセージ関係はまだ上手く動いていないみたい )

    引数の区切りを二つ見ていたのを、最初の一つしか見ないようにした。
    ( コマンド入力部の引数は、登録されている引数の後に追加される )

    キーワードを自動補完しない設定のときの、補完の挙動を調整

    登録コマンドの実行でエラーが発生したときには、エラー番号を出す
    ようにした ( エラー番号の意味は manual.txt に記載 )

    キー割り当てと空欄コマンドが競合しているときに、警告を出すよう
    にした

    メニューキーのデフォルトを、Ctrl+Return から、Ctrl+Shift+Spaceに
    変更 ( 空欄コマンドとの競合を避ける為 )

    Windowsの終了でCraftLaunchが終了すると、履歴が保存されない
    不具合を修正


◎ 1.64 :
    状態表示部のダブルクリックで出たコマンドメニューでの実行で、
   【コマンドを実行でウインドウを隠す】がうまく行ってなかった
    不具合を修正

    ショートカットをドラッグ＆ドロップして登録すると、代わりに
    実体を登録するか、ユーザに問い合わせるようにした

    %SubCommand で複数のコマンドを実行しようとして、最初のほうで
    エラーが発生したとき、残りのコマンドが内部に残り、その後
    しばらく【 ER 】が表示される不具合を修正

    clnch.exeの最初の起動でも、コマンドラインオプションをちゃんと
    反映するようにした

    コマンドラインオプションで、コマンドを一つも書いていないと
    死んでしまう不具合を修正

   【起動直後はウインドウ非表示】オプションを追加


◎ 1.62 :
    %cb と %pwd が使えなくなっていた不具合を修正

◎ 1.61 :
    カレントディレクトリに置換される特殊変数【 %pwd 】を追加

   【コマンドを実行でウインドウを隠す】オプション設定時、
    %PWDの後にウインドウが非表示になる不具合を修正

◎ 1.60 :
    カレントディレクトリを表示する特殊コマンド【 %PWD 】を追加

    %CD などの特殊コマンドを手動で実行し、エラーが発生したときに
    ?? と表示されていたのを、正しく ER と表示するようにした。

    ドラッグ＆ドロップでの登録で、メニューへの追加が無視される
    不具合の修正


◎ 1.59 :
   【非アクティブでウインドウを隠す】の挙動がおかしいので、
    原因が分かるまで無効

◎ 1.58 :
   【コマンドを実行でウインドウを隠す】オプションを復活

   【コマンドを実行でウインドウを隠す】オプション設定時、
    %PopHistoryMenu 直後に CraftLaunchが非表示にならないようにした

   【非アクティブでウインドウを隠す】と、
   【非アクティブ時も入力内容を維持】が設定されており、
    コマンド入力部が空でない状態で非アクティブになったとき、
    ウインドウを隠さないようにした


◎ 1.57 :
    コマンドラインからのコマンド入力時／メニューのポップアップ時に、
    ウインドウが非表示のままになる不具合を修正。

◎ 1.56 :
    コマンドラインからのコマンド入力で、ウインドウを一時的に
    移動させていない場合も、移動させたときの挙動になっていた
    不具合を修正

   【コマンドを実行でウインドウを隠す】オプションを廃止

    コマンドラインからのコマンド入力は、補完しないようにした


◎ 1.55 :
    履歴一覧メニューを表示する特殊コマンド
   【 %PopHistoryMenu 】追加

    コマンドを DELキーや BSキーで編集するときに、検索インデックス
    がリセットされない不具合を修正

◎ 1.54 :
    CraftLaunchのウインドウにキャプションを設定するようにした
    ( ウインドウ操作補助系アプリから制御できるように )

    コマンドメニューに表示するかどうかを、コマンドごとに設定
    できるようにした


◎ 1.53 :
    手動入力の引数の末尾にスペースが入る不具合を修正

◎ 1.52 :
    カレントディレクトリを変更する特殊コマンド【 %CD 】を追加

    コマンドラインからの複数の入力のうち、最後の入力以外を実行
    するようにした

    特殊コマンドの実体をコマンド入力部に入力できるようにした

    特殊コマンド【%Setup】に引数を渡せるようにした


◎ 1.50 :
    Windows98/2000 で CraftLaunchがアクティブにならないことがある
    不具合を修正

    コマンドラインから、ウインドウの位置/サイズを一時的に変更する
    際の、ちらつきを押さえた


◎ 1.48 :
    コマンドラインから CraftLaunch の横幅を変更する
    コマンドラインオプション【 /w???? 】追加

    他にウインドウがない状態でESCキーを押すと、
    CraftLaunchが反応しなくなる不具合を修正


◎ 1.45 :
    コマンドラインからの入力後の、選択状態を細かく設定する
    コマンドラインオプション【 /c0 - /c9 】を追加

    コマンドラインからウインドウ位置を変更するコマンドライン
    オプション【 /x???? /y???? 】追加

    ウインドウ位置を 直前にアクティブだったウインドウ内での
    割合（ 百分率 ）で指定するオプション【 /wr 】追加

    引数が勝手に大文字に変換される不具合を修正

    クリップボードの文字列を取得する特殊変数【 %cb 】を追加


◎ 1.42 :
    コマンド本体部分のみ補完する機能が自動的に行われるようにした

    該当するすべてのウインドウにメッセージを送る特殊コマンド
         【 %WndTitleMsgAll 】【 %WndClassMsgAll 】を追加

    Windowsの終了でCraftLaunchが終了すると、履歴が保存されない
    不具合を修正


◎ 1.40 :
    入力済みの引数を残したまま補完できるようにした。

    引数が入力されていても、コマンド本体が空欄だったら、
    空欄コマンドを実行できるようにした。

    /chなしのときも、カーソルが先頭に行ってしまう不具合修正


◎ 1.39 :
    CraftLaunchへのコマンドライン引数を、入力部への入力と
    みなすようにした

    CraftLaunchへのコマンドラインオプションに、即実行する【/ex】と、
    カーソルを先頭に持って行く【/ch】を追加

    D&Dによるコマンド登録時に、カーソル位置に追加されるようにした


◎ 1.38 :
    コマンドを連続実行する特殊コマンド【 %SUBCOMMAND 】を追加

    コマンドラインからのコマンド実行で、タスクトレイアイコンが
    増えていく不具合の修正

    履歴が追加されないことがある不具合を修正


◎ 1.36 :
    CraftLaunchへの引数をコマンド入力として実行する機能の追加

    入力途中でフォーカスが外れても入力内容を維持するオプションを追加

    一部の空欄コマンドが動かない不具合を修正


◎ 1.35 :
    非アクティブ時にウインドウを隠すオプションの追加
    ( 起動直後ウインドウを隠すを廃止 )

    空欄コマンドを履歴に追加するかどうか設定可能に

    設定ツールのコマンド一覧の動作の調整

    コマンド一覧メニューにセパレータを挟むことが可能に


◎ 1.33 :
    何も入力しないでリターンしたときに指定した
    コマンドを実行する『 空欄コマンド 』機能の追加

    コマンド一覧メニューを表示する特殊コマンド【 %PopCommandMenu 】
    を追加

◎ 1.32 :
    ダイアルアップコマンドが実行されない不具合の修正

    ウインドウ位置をマイナスにするとウインドウが
    小さくなっていた不具合を修正


◎ 1.30 :
    設定ダイアログにウインドウ位置取得ボタンの追加

    バグ修正

◎ 1.29 :
    重複した履歴を削除するオプションの追加

    自動補完をON/OFFするオプションの追加

    情報表示機能を自由にカスタマイズする方法にした

    ウインドウキーをホットキーで使えるようにした

    その他調整


◎ 1.27 :
    すべてのコマンドでパラメータを受けとるようにした

    1.25で発生した不具合の修正

◎ 1.25 :
    ファイル名指定実行において、パラメータを受け取るようにした

    スペース文字を入力できるようにした

    アクティブ化ホットキーで、入力をリセットするようにした

◎ 1.24 :
    CraftLaunchがフォーカスを得たときに自動でIMEをOFFにする
    オプションの追加

    補完キー、情報切替えキー、をカスタマイズ可能に

    コマンドエラー時に、"ER"を出すようにした

◎ 1.20 :
    コマンドをドラッグ&ドロップで登録可能に

    起動直後ウインドウを表示しないでメニューを表示させると、
    意図しない位置に表示される不具合を修正

    失敗したコマンドは履歴に加えないよう変更

    DelやBS直後に補完してしまうことがある不具合を修正

    その他調整

◎ 1.18 :
    ファイル名を指定して実行する機能の追加

    CraftLaunch起動直後に非表示にするオプションの追加

    [画面のプロパティ]→[ディスプレイの詳細]→[フォントサイズ]
    で、小さいフォント以外を指定した際に表示が変になる不具合
    を修正

◎ 1.16 :
    ウインドウの横幅を設定可能にした
    ヒストリが逆に保存される不具合を修正
    非アクティブ時に情報を表示しない選択肢を追加

◎ 1.15 :
    コマンドの履歴を上下キーで呼び出す機能を追加

◎ 1.12 :
    ウインドウを最前面表示するオプションを追加
    デフォルトでどの情報を表示するかを選択可能に
    起動中のアプリにメッセージを送信する機能の追加
    タスクトレイアイコンを表示しないオプションを追加
    コマンドのコピー登録機能を追加
    小文字を含むコマンドに[OK]が出ない不具合修正
    コマンド一覧メニューの最大数を64から1024に拡張
    その他調整

◎ 1.08 :
    スペースキーで次候補選択する機能
    アプリ起動直後にウインドウを隠すオプションを追加

◎ 1.06 :
    コマンド一覧メニュー機能を追加
    正式に公開

◎ 1.04 :
    コマンドメニュー機能を追加
    コマンドの優先順位をつけた
    システムトレイにアイコンを置いた
    その他、細かい調整。

◎ 1.01 : コマンドに引数を渡す機能の追加

◎ 1.00 : 公開

